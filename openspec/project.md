# Project Context

## Purpose

本项目是求职向 Gameplay 客户端程序 demo。目标不是完整 PvPvE 产品、MMO、纯网络框架或通用编辑器产品，而是展示第三人称动作客户端能力：输入响应、角色控制、相机、动作状态、动画表现、战斗窗口、受击反馈、调试可视化，以及网络模型压力下仍可拆分、替换和审查的玩法模拟边界。

业务压力场景固定为一个 2v2vE 动作战斗技术演示：两名真人玩家、两名使用同一角色管线的玩家角色 Bot，以及不属于双方队伍的中立怪。重点不是完整比赛规则，而是动画表现、操作手感、伤害结算，以及两名真人玩家围绕同一个战斗事件进行高实时互动。正式范围与验收口径见 `openspec/2v2ve-gameplay-client-demo.md`。

当前重心是以 BTSMTL 作为 authoring 底座，将 Graph、StateMachine、Timeline、TreeClip、Blackboard、Action、GameplayEffect 和 motion 数据编译成 portable Gameplay Program。Unity 角色表现、世界求解和后续网络模型必须装配在同一条 Program/Session 链路上，不恢复旧 SO/config、对象解释器或 Character stage 分裂路径。

## Current State

- `openspec/specs/` 当前包含 88 个 current spec，并与本文件共同表达已安装架构。active change 只记录尚未完全收口的实施增量；工作区中孤立的新类型、未接入的模块或未完成的配置不能单独成为 current truth。archive 只记录用户已验收并完成收口，当前架构仍只以本文件和 current specs 为准。
- active `complete-character-predictive-foot-ik` 当前已完成 Landing Event 实时更新合同和每脚唯一最终 Goal 换代的代码接入：Landing、Ground Path 与 Envelope 只表达本帧当前有效预测，最终 Goal 只保存相对同帧原生踝骨的 Committed/Pending 修正并向最新目标连续收敛，不缓存第二条 Path。2026-08-20楼梯样本先暴露旧Pelvis空间重复叠加与Stride Start重基造成约0.815米单帧Goal跳变，后续样本又捕获支撑/坡向换代后旧Spring Velocity继续推向旧方向的稀有上冲和6Hz响应过快形成的持续上下晃动。工作区现以`sampledGround - poseRootPosition`和最终加权双脚Sole抬升建立唯一临界弹簧Target，通过typed handoff只清除远离新Target的旧速度，并把Corin与TrainingEnemy正式Pelvis Spring Frequency统一为3Hz；不存在第二Pelvis Goal或额外淡入淡出。该修正仍待用户重新采样确认；Ground Envelope、Swing Foot Motion、Goal换代后的物理踝骨、Pelvis、支撑锁脚、脚掌朝向、Pivot、FullBodyIK消费和最终Physical Bone写入仍不得描述为已验收。
- Corin RootTree、nested StateMachine、Timeline、TreeClip、Blackboard、Action、GameplayEffect、motion curve 已由唯一 Frontend 编译为 numeric-neutral Semantic IR。其 canonical artifact 固定写入 `Library/CharacterSimulation/SemanticIr/<definition-guid>.csir`；Editor从该artifact建立唯一`CharacterPresentationSemanticContract`，再独立生成target-neutral `CharacterPresentationProjectionAsset`与Build Request显式声明的Float32/Fixed Target Program及wrapper。Projection不再等待或反读Float32 Program。
- `CharacterPipelineDefinition`代码与authoring合同已收敛为角色配置装配根；Pose Graph唯一拥有typed Source Slot子资产，`CharacterAnimationPresentationProfile`唯一拥有对应Binding、动画资源、有限Action binding、节点Policy、Rig、FullBodyIK Profile与Foot Analysis Source。Projection只发布dense source index，Runtime不查作者字符串。Corin正式链固定为`Component Pose -> FootPlacement Final Goal Set -> 唯一FullBodyIK -> OutputPose`；current spec描述了Ground Path、Swing Foot Motion与唯一final writer诊断的正式边界，但本轮用户只确认Accepted Landing、落点身份和Ground Path线，不能据此认定后续模块已经通过。Rig v4唯一声明Physical/Virtual Bone和FBBIK biped映射。
- PoseStateMachine Transition的混合作者合同已经收口为edge-owned `Blend Logic + Duration + Blend Mode + 条件式Custom Curve Asset + Blend Profile Asset`。`Blend Logic`只包含Standard Blend与Inertialization，BlendStack仍是显式Pose Graph节点；Standard Blend在Native Pose阶段按canonical curve和dense per-bone profile求值，StateMachine触发的Inertialization读取同一edge数学，节点Policy只保留history/residual/parameter response。Slot、显式BlendStack与直接Player Inertialization Policy统一使用`Blend Mode + Custom Curve Asset`作者格式，不再保存第二种inline曲线或Transition自由文本curve/profile identity。
- 角色专属Custom Blend Curve正式目录固定为`Assets/Configs/Character/<Character>/Pipeline/Presentation/BlendCurves/`，资产名使用`<Character><Purpose>BlendCurve.asset`。能被Linear、EaseIn、EaseOut或EaseInOut精确表达的曲线必须使用内置Mode，不创建重复Curve Asset；只有真实自定义形状才进入该目录并通过稳定CurveId与revision参与Projection依赖。
- `add-character-motion-matching-pose-source`的工作区已经收口Profile、Source Set、Foot Analysis、Database Build、`.mmdb`、Projection payload compiler、Trajectory、Query、Search、Plan、Selection、state-local ABI与表现Module。它们尚未被一套正式内容配置装配起来，也没有角色运行这条链。完成接入后，唯一边界是`PoseState relevance -> CharacterMotionMatchingPresentationModule -> PresentationPoseSourceSample -> explicit Player -> 可选局部Inertialization或显式BlendStack -> Pose Plan`；MM不得拥有Gameplay channel、Program producer、PlaybackId、第二播放器或Body root motion。该能力仍是active增量，current specs尚未安装“项目具备可选MM能力”。
- 当前项目没有完成Motion Matching正式内容接入。工作区存在项目自研MM的Runtime、Editor与Projection编译基础设施，但没有任何可运行角色同时具备正式MM Profile、Source Set、Database Artifact、provider binding、Projection payload与PoseState Player接线；Corin的`m_MotionMatchingProfile`为空，生成Projection的MM开关为0且Clip集合为空，因此运行时不会构造MM Module，也不会产生MM source sample。不得把“类型和代码存在”描述为“角色已经接入”。
- `Assets/Plugins/MotionMatching`中的第三方MxM同样没有接线，只作为内容与实现参考存在；当前GameScripts、Character Config和Projection不引用`MxMAnimator`、`MxMSearchManager`或`MxMAnimData`。项目正式接入不得并行启用MxM的Trajectory、FSM、Mixer、Layer、Transition、Root Motion、PlayableGraph或更新时序。若以后复用MxM离线数据，必须由独立Editor-only显式Importer change转换成项目正式Database Artifact，不能让MxM类型进入Runtime Projection。
- Equipment Core 已作为可选Character capability安装：Definition只引用`CharacterEquipmentProfile`与`CharacterEquipmentPresentationProfile`；Slot、Route、Equipment、Feature、Initial Loadout、Persistent/Route inline graph与typed参数由唯一Frontend编译进同一Semantic IR及Float32/Fixed Program。每Actor装备真相只存在于`CharacterSimulationState` Equipment aggregate，外观只消费committed visual selection与Presentation Projection。Corin、Agent schema与Network Model尚未接入该能力。
- active `add-linked-pose-interface-runtime` 已在工作区形成通用 `Group + Interface + Implementation + Entry + selector adapter` 能力：Equipment 只是首个 selector，核心 selection frame 与 generation runtime 不含 Equipment switch。全部候选 Entry fragment 在 Character Build 时进入同一 Presentation Projection、dense source 目录与 Native Pose Plan，并在唯一 `Prepare -> Validate -> Animancer Evaluate Barrier -> Seal/Discard` 事务中切换；StateMachine、player、Motion Matching、inertialization与Root Orientation Warp继续使用各自正式typed owner，不存在通用node-state字节页、第二executor、运行时Graph解释、资源路径查找或fallback。正式Empty Implementation同时提供Local Pose passthrough与Ready/GoalCount=0的FullBodyIK Goals。Corin正式EquipmentSlotId/EquipmentId尚未迁移，因此当前只交付能力与Empty资产，不建立临时武器业务接线；该增量须在用户端到端验证并archive后才并入current specs。
- 当前正式单机链路是 `GameplayTickSystem -> SimulationSessionHost -> Local Source -> compiled Standard Local Pipeline -> Ingress -> Schedule -> Evaluate -> World ResolveBatch -> Finalize -> Egress -> atomic Commit -> Presentation`。
- 当前动画运行已经分离Simulation Tick与Presentation Tick：Gameplay Timeline只在Simulation Tick推进logic time并提交有限Action的committed raw sample锚点；Presentation在相邻锚点之间按presentation delta投影visual time，再执行source-local Marker映射、Clip采样、PoseState、Slot、ordered Pose stages与最终Pose。Sample锚点不是骨骼Pose，也不是固定Tick直接驱动Animancer的逐帧命令；表现投影不得写回Timeline、Window、Motion、Warp、Cue或Action lifecycle。持续Locomotion只由Presentation Fact与PoseStateMachine选择state-local source，不再由Gameplay Timeline提交BaseLocomotion producer。动画表现帧使用唯一`Prepare -> Validate -> Animancer Evaluate Barrier -> Seal`事务：Dense Pose与Native/Inertialization/Final结果使用预分配Committed/Pending双页，稀疏Action与source生命周期使用固定journal和prepared/deferred resource；正常帧不创建完整状态或Physical Bone before-image。Barrier前失败只丢弃Pending，Barrier期间或之后失败使该Actor Animation Runtime进入Faulted；Diagnostics只在存在interest时从成功Seal的Committed页复制。Corin作者资产已迁移到Rig v4、Calibration v4和显式Pose空间；generated Projection与Float32/Fixed Numeric Target Program只由明确Character Build命令发布，不自动构建。
- 当前已安装BTSMTL AI核心：`AIControllerDefinition`显式装配`AIControllerTree`、受控Character Definition、Perception Profile和generated `AIIntentProgram`；独立AI窗口只提供领域上下文，GraphView、Inspector基础、Data Catalog、页栈、Undo和Live Debug继续复用`BaseTreeWindow`。AI Frontend把Graph降低为portable AI Semantic IR，Float32 runtime复用唯一`OperationControlRuntime`并只输出`CharacterSimulationInput`。Standard Local Pipeline的唯一Control Input Ingress从上一轮committed World State构造统一Actor Observation，在同一outer transaction内准备Player、Neutral和AI输入，并把AI Controller State纳入checkpoint、snapshot、hash与restore。该能力当前只安装Local Float32；统一Agent Document v3目录包通过显式`CharacterController`/`AIController` domain覆盖AI Graph、Blackboard、Perception、Intent、事务与Validator，不负责创建不存在的root，也不保留bootstrap旁路。Corin训练敌人的`Corin.TrainingAI` Definition、显式`LocalActor` Perception、AI RootTree与训练行为authoring已经存在；Character作者资产正在随Rig v4与Calibration v4迁移，Foot Analysis、Projection与Character Program前置产物必须在IK change闭合后重新发布，AI Program与Document v3正式validate仍由`add-corin-training-ai-demo`的剩余任务收口。在AIIntentProgram重新发布并验证前，不得手工回绑旧Program或回退Neutral。
- 唯一`Tools/3C/Launcher`按业务目的分组。当前已安装入口包括Local Float32与Local Fixed Gameplay Lab、三个Network Test Product的Build/Run和正式Content/Player构建运行。商业启动代码已经形成`Bootstrap -> ProductBootstrapRunner -> GameApp.Entrance -> ProductStartupCoordinator -> ProductShell -> Login -> Home -> Gameplay Preload`结构，但正式`ProductStartupProfile`尚未配置唯一HTTPS ResourceEndpoint与WSS AuthEndpoint，共享字体/材质/图集的正式打包规则也未完成，因此该链当前不得描述为可发布运行的完整产品闭包。`Assets/Scenes/GameplayLab/GameplayLab.unity`只属于Editor/Development技术展示，不进入商业启动、资源分包或认证链。
- 客户端正式构建产物只使用 `Build` 根，并严格分为 `Build/Content/<BuildTarget>/DefaultPackage/<ResourcePackageVersion>`、`Build/Players/<BuildTarget>/<ClientBuildVersion>` 与既有 `Build/Network`。`Build/.Workspace` 只保存商业构建原始输出、OutputCache、BuildReport 和候选目录；YooAsset 默认 Builder 与 EditorSimulate 只写 `Library/YooAsset/BuildOutput`。`Builds`、根 `Bundles` 和无版本普通 Player 目录已删除，不提供迁移、镜像或 fallback。
- `SimulationSessionHost` 是唯一运行装配根，显式组合 Program Runtime、Execution Backend、Pipeline、Session Source 与 WorldSolver，拥有 preparation、compiled runtime handle、锁定 roster、Input/Logic target、World state 和销毁顺序。
- Float32 的唯一正式组合入口位于 portable Float32 source set：它编译 Pipeline、创建 Backend request 与 LaunchPlan。每份 Float32 Pipeline Definition显式提供包含descriptor、portable factories、runtime factories与Product runtime的neutral package；Prepared Source显式提供一次性Runtime Launcher。Unity Composer只把五项Definition、Actor port、Source、Solver、输出和诊断降低为一个portable request并调用该Launcher，不识别具体Network Model或拥有第二套组合规则。
- `CharacterPipelineHost` 只负责 Actor registration 与 Presentation：绑定 Program/Projection、Input Adapter、Actor body、visual/camera 和 diagnostics ports，再向显式 Session Host 注册；它不创建 Source、Kernel、Solver、Backend、Pipeline runtime 或 Logic target。
- Gameplay可变状态集中在typed immutable `CharacterSimulationState`与`WorldSimulationState`。Runnable、StateMachine、Timeline、Blackboard、Input request、Action和GameplayEffect都由Program operation与typed state address推进；每个Actor/Step只创建一个target-specific State Transaction，并从Evaluate延续到Finalize后唯一Commit。
- 当前安装 Float32 Numeric Target与DeterministicRollback专属FixedQ32.32 Target。两者拥有独立Program/State/Numeric ABI，但复用portable Runnable/StateMachine、Timeline、GameplayEffect准入/生命周期与Pipeline Transaction控制；Target只实现数值、曲线、typed state和输出投影。ProgramExecutionServices按Program构建一次，Session/Actor workspace复用Tick容量，跨事务Snapshot、history与输出在发布前独立冻结。Fixed Program/State/Kernel、包含`ActorCollision`的Deterministic KCC、canonical input、完整world snapshot history/replay/hash与EventId output disposition已通过独立Rollback组合接入；Fixed ABI不进入Local、ServerAuthoritative或DotRecast组合。Rollback KCC源码已经以Philippe St-Amand Kinematic Character Controller为主要movement policy参考：Fixed upright capsule对Plane、Box和one-sided indexed Triangle执行canonical Cast、Raycast与Overlap；唯一Motor执行初始去穿透、previous stable ground上的负Y已积分位移约束、Philippe有效障碍法线、坡面切向投影、多平面slide、统一Hit Stability、Standard-first/Extra Step、Ground Probe、ledge/denivelation与Actor Contact后静态重约束。Motor使用全部active planes统一枚举原始位移、单平面投影、双平面交线和零向量，按Fixed距离与candidate identity稳定选择合法remaining；连续两轮相同`TOI=0`、safe position、canonical contact和不变投影会确定收敛为`BlockedNoProgress`，继续Ground Probe与最终validation，而事实仍变化的iteration耗尽继续失败。Collision baker为每个作者Collider生成稳定SurfaceId，同一MeshCollider或TerrainCollider的Primitive共享该身份；写Artifact前会拒绝支撑轴不平行、上表面水平投影正面积重叠且OBB正体积穿插的walkable Box pair。Gameplay Lab粗糙区域已改为唯一连续`RoughGroundCollision`顶面Mesh，周边`CourseGroundCollision`在该区域开孔，108个可见RoughTile不再拥有Gameplay或Foot Placement BoxCollider。独立Step Solver、独立Step Down、微距Ground Snap、旧SkinWidth/MinimumStepDepth配置和旧codec reader已经删除；KCC只保存会影响下一Tick的Fixed Gameplay ground/support状态，不拥有Position、VerticalVelocity或任何动画、Presentation、Timeline事务。源码身份已经升级为Motor `fixed-philippe-kcc-motor/9`、WorldSolver `10`、KCC schema `deterministic-kcc/8`、Collision Artifact `deterministic-collision-world/3`；当前CollisionWorldHash为`19d2b81df6204842d2d45b0517125d2d96bc9ff6f9423c5205492d33f98e7097`。Local Fixed与DeterministicRollback Variant继续引用同一KCC配置和collision artifact，只在Session Source与Network Model装配上不同。每个Actor拥有预分配Motor/query workspace，batch再按stable ActorId pair执行`SolidBodyBlock`、同一Motor静态重约束和原子提交。DotRecast authoritative backend继续通过同一Float32 Program、Pipeline和Session Composer接入。
- `optimize-deterministic-kcc-contact-resolution` 已将 KCC movement representative、Step admission、closest-only Raycast 策略纳入当前实现；Motor `fixed-philippe-kcc-motor/10`、Query `fixed-capsule-ray-conservative-cast/5`、WorldSolver `11` 与 KCC identity `deterministic-kcc/9` 是当前版本。该优化只属于 Deterministic KCC，不改变 Tick、其它 WorldSolver、Collision Artifact、动画、Foot Placement、Presentation 或 Network Model。
- Gameplay Lab连续Low、High与OverLimit往返楼梯已经使用正式双表面作者数据：每条路线的上行与下行各有一个持久化无Renderer Traversal Ramp，共六个`CharacterTraversal`梯段；Fixed Collision Artifact只消费这些Ramp，真实可见踏面保留原尺寸并迁入`FootPlacementSurface`，只供Foot Placement查询，顶平台继续作为共享`Ground`。环境根的宽泛Deterministic Surface marker已经删除，普通地面、坡面、墙体、阻挡与楼梯Ramp分别归属显式Surface作者根；独立`StepCapabilityCourse`使用0.14m、0.24m与0.40m真实孤立障碍保留KCC Step准入和拒绝边界。Corin Foot Placement查询Mask为`Ground | FootPlacementSurface`并排除`CharacterTraversal`。唯一Deterministic Collision World artifact于2026-08-03 13:30:42通过Unity Editor正式菜单重新Bake，当前`CollisionWorldHash`为`d2921f1e50a1a7722f8a9d8762eef8038f7f9cad66185ac32998788ed3c1d7c3`，资产文件SHA-256为`9b5e6350d0dc262e710afd4ce19f9806b5645dc88805970d28c1e4f0c78428b3`；canonical数据包含六条Ramp和三个独立Step障碍，不包含三条路线的真实踏面Collider。Local Fixed与Rollback Variant继续引用同一Collision Asset GUID；该楼梯表面change已归档，现有Network Product仍需按Local Fixed、Relay和Peer A/B顺序显式重新验证。
- 当前安装显式 Float32 Local、ServerAuthoritative Prediction 与 ServerAuthoritative Authority 三类正式 Pipeline。ServerAuthoritativeHybrid同时提供Unity Authority四进程纵切和DotRecast Authority三进程纵切；两者复用同一网络模型、Source、Pipeline、Checkpoint、Correction与UDP数据面，旧 LocalLoopback capability与实现均已删除。
- Fantasy Server 已拆成共享 `ThirdPerson.Server.Host`、共享 Gate Entity/Hotfix 和两个产品专属模块。Unity Authority 由 `ThirdPerson.UnityAuthority.Server` 发布，只包含 Gate 与 external Worker route；DotRecast Authority 由 `ThirdPerson.DotRecastAuthority.Server` 发布，包含 Gate、进程内 Authority Scene 与 portable DotRecast 闭包。两者没有通用 `Main.exe`、运行时产品切换或共享 concrete Router。
- 三个 Network Test Product 通过唯一Editor-only `NetworkTestProductBuildWorkflow`和显式adapter构建，分别发布到互不覆盖的`Build/Network/UnityAuthority`、`Build/Network/DotRecastAuthority`与`Build/Network/DeterministicRollback`。公共workflow唯一拥有外部进程、临时目录、原子替换、exact file closure与产品manifest校验；`ServerProductBuildManifestUtility`唯一拥有Fantasy服务端manifest/hash。Unity Authority与DotRecast adapter各自发布匹配的Fantasy Server产品，Rollback adapter发布独立的纯.NET Dedicated Relay Server artifact与portable runtime manifest。Run只验证并启动既有产物，不编译、不publish、不修复目录；所有Server文件日志必须写入当次`Build/Network/RunLogs/<Model>/<RunId>`，不得修改已校验的ProductRoot。
- `refactor-gameplay-session-composition-boundary`已归档；公共 composition基座已成为 current spec，可供后续网络模型复用。DotRecast与 Deterministic Rollback不得复制 Host、Pipeline compiler或 Actor registration。
- GitHub CI 由三个无依赖并行 job 组成：`Repository Policy` 校验 Git 跟踪边界和 Unity `.meta` 配对，`OpenSpec Validation` 严格校验全部 current specs/change，`Portable Unit Tests` 编译 Core/Float32 并运行 NUnit 合同测试。
- portable NUnit 当前覆盖 CanonicalData 非法输入与 round-trip、StableHash/EventId identity、ProgramCurve 排序/求值/codec；测试工程只引用 Core 与 Float32，不引用 Reader、Unity 生成工程或 Unity API。
- 客户端主目录是 `3cDemo/Client/3C_Client`；服务端代码按 `3cDemo/Server/Shared`、`Gate`、`Products/UnityAuthority`、`Products/DotRecastAuthority` 与 `Startup` 分层。`ThirdPerson.Startup.Server` 只装配 AuthGateway、Auth Entity/Hotfix、正式 Outer 协议与共享 Server Host，不进入 Network Test Product catalog。AuthGateway 只保证单 Scene 内 `AccountId -> Session identity + ClientInstanceId + Generation + token identity` 的唯一当前记录。Fantasy Gate Scene继续拥有 ServerAuthoritative Demo Room和控制路由但不执行 Gameplay；独立DotRecast Authority Scene继续拥有portable Program、Pipeline、Source、Solver、World与UDP数据面。Entity 目录中的 `Fantasy.config` 只服务代码生成并被产品排除发布，各产品目录中的源 `Fantasy.config` 才是部署真相；`Ref`仅作为参考代码，不是运行时依赖。

## Tech Stack

- Unity 2022.3.62f2c1。
- C#、UI Toolkit、GraphView。
- URP 14、Cinemachine 2.10、Unity Input System、Unity Timeline。
- BTSMTL / TreeDesigner / Timeline 本地代码。
- Animancer只负责在正式PlayableGraph中复用、采样和维护source playable寿命。动画表现由编译Pose Plan显式安排`SelectedPosePlayer`、`BlendStack`、局部`Inertialization`、Pose组合、Local/Component Pose转换、统一`FootPlacement`与`PoseBoneIKGoals`目标生产、唯一`FullBodyIK`求解和最终发布；Marker映射属于source sample时间解析，不是Pose Graph节点。没有对应节点时不得后台补建连续性、MarkerSync、IK组件或后处理。
- Fantasy.Unity / Fantasy.Net 骨架。
- OpenSpec 用于能力规划、实施和归档。
- GitHub Actions 使用显式 `windows-2025` runner；OpenSpec job 固定 Node 20.19.0 与 `@fission-ai/openspec` 0.23.0。
- portable 测试使用 .NET 8、NUnit 4.6.1、NUnit3TestAdapter 6.2.0 与 Microsoft.NET.Test.Sdk 18.8.1，不进入 Unity Test Framework。

## Architecture

### Gameplay Client Direction

- 对外作品口径是 `Network-aware Third Person Action Combat Prototype`，第一目标是可运行、可调试的 Gameplay 客户端纵切。
- Authoring 编译链固定为 `CharacterPipelineDefinition -> Authoring Discovery -> Semantic Emission -> validated .csir artifact -> CharacterPresentationSemanticContract -> { Presentation Projection Compiler | requested Numeric Target Adapters } -> cross-artifact validation -> atomic publish`。Definition 只装配正式 Config 引用；Animation Presentation Profile、Equipment Presentation Profile与正式Analysis artifact只进入Projection分支。`.csir` 是可删除、可重建、不进入 Assets/Player 的generated artifact，不是第二份authoring真相。Semantic IR只存在于Editor/普通.NET编译和诊断边界，Runtime不解释IR，也不在Program过期时回退authoring object。
- Program 是 Gameplay 真相，Projection 是 Unity 表现资源绑定。Float32与Fixed Program分别拥有ProgramHash、LayoutHash、NumericProfile、Target ABI与State codec；Projection只保存ProgramId、Gameplay SourceRevision、SemanticHash、ordered producer contract对应的ContractHash和独立ProjectionRevision。Host先严格加载所属Target Program，再由对应Adapter建立Presentation contract并加载同一Projection；任一Program或Projection身份不匹配都直接拒绝启动。
- Editor `CharacterSimulation` Module拥有Build Request、Target Adapter、Projection Compiler、numeric-neutral Presentation Semantic Reader、Analysis artifact resolver与原子发布事务；Runtime只保留不可变Projection payload、Target Contract Adapter和消费接口，不引用AssetDatabase或authoring编译实现。
- 每个 Session 锁定 Program Runtime、Execution Backend、Pipeline、Session Source、WorldSolver、ProgramCatalog、Snapshot codec 与 Actor roster。Runtime 不按 Network Model、Actor、Node、packet 或 Tick 切换组合或数值 backend。
- Session 在 Active 前把 portable Pipeline descriptor 编译为 immutable plan。每个 outer LogicTick 依次执行 Ingress、唯一 Schedule producer、零到多个有序 Step、Egress 与一次 atomic Commit；任一阶段失败都不得发布部分 Character/World state 或外部输出。
- Session 的 Tick 0 Pipeline state 必须从已经 Activate 的 SnapshotParticipant 正式捕获，或从调用方显式提供的完整 snapshot 原子恢复后重新捕获；不得用空 participant 数组伪造初始 Pipeline state。
- Session Source 只拥有输入、端点、外部队列、模型 history 资源和 Source ports；Pipeline Pass 显式决定 ingress、restore/replay/current schedule、Evaluate/WorldSolve/Finalize 及 output disposition。Source 和 Pass 都不得直接执行 operation、修改 Transform 或播放表现。
- 本地 Input Adapter 将设备输入降低为 Float32 `CharacterSimulationInput`。Vector2输入可显式选择数字方向冲突策略；Corin `MoveAxis`用唯一Dpad四方向part的回调与采样边沿维护最近激活方向，只在同轴正负键同时按下时替换Unity相消结果。解析发生在Camera-relative转换和portable input构造前，Fixed、Rollback history与网络继续消费同一已解析输入，不保存物理按键历史。Graph/StateMachine/Timeline/Action 读取 Program input/state，不直接读取 InputAction、Unity Time 或场景对象。
- Gameplay Effect与Action生命周期已进入同一个typed `CharacterSimulationState` layout；Input request、Action request/instance/reference、target snapshot与唯一GameplayEffect aggregate都有明确value kind。`Float32OperationEvaluator`通过窄领域port读写同一`Float32CharacterStateTransaction`并输出稳定EventId的GameplayFact、PresentationCommand与Trace，不再由Character stage、Builder或opaque bytes持有第二份运行状态。
- Numeric-neutral Value Port Contract是Value输入的唯一类型真相。Frontend将已连接输入编译为Value edge、未连接输入编译为结构化constant-input binding；Float32/Fixed Program Layout在composition阶段建立连续input span、Timeline/State owner、reference、named constant、catalog和SourceMap索引，Tick内不得再解析字符串端口、排序或扫描完整Program。
- Program固有的`ProgramExecutionLayout`与backend固有的`KernelProgramBinding`分离；Kernel只接受与Program/Layout/backend identity精确匹配的binding。当前Float32 Program使用ABI 7，committed State使用`character-state/float32/v8` canonical codec；Fixed Q32.32 Program使用ABI 6，committed State使用`character-state/fixed-q32.32/v7` canonical codec。Program ABI、Character State codec、World/Snapshot codec与Operation Set是相互独立的版本身份，必须从各自正式常量建立兼容性，文档不得用旧组合推导当前产物。
- Evaluate通过Actor workspace持有page ownership transaction、typed Blackboard owner/provenance与唯一output lease；Finalize成功后一次冻结最终Result并发布唯一canonical Pipeline candidate。Snapshot、Hash与Network Baseline只读取Finalize成功后的committed State并保存Program/Layout/codec identity；active Transaction、Pending lease、GameplayEffect working view与当前Step Motion transient不得进入History、packet或Snapshot。

### Motion Direction

- Program Evaluate先收集输入locomotion、Timeline MotionCurve与业务位移的`SimulationMotionContribution`，按固定channel解析为`ResolvedMotionChannel`，执行Operation Set声明的Motion Modifier，再按固定channel顺序合成唯一`ResolvedGameplayMotion`，全过程不修改Transform。`TimelineMotionWarp`是当前唯一正式Modifier，只替换成为Action channel resolved owner的显式ActorLocal source MotionCurve部分并保留同channel additive贡献；它从对应ActionInstance读取激活时固定的目标快照，以Translation Mode、Target Offset Space、Rotation Mode/Method和Limit Policy建立累计轨迹上下文，将窗口开始Body pose、源窗口起始pose、有效Target Pose、previous Warped Cumulative Pose、generation、ActionInstance、source identity与累计progress保存在typed Character State。每Tick只提交相邻Warped Cumulative Pose之差，不因碰撞追补；raw contribution、resolved channel、resolved gameplay motion、Body Motion plan与最终request等同Tick transient不进入committed State、Snapshot或StateHash。
- 当前Numeric Target的唯一Body Motion Integrator在全部玩法Motion与Modifier完成后执行Prepare：读取compiled `CharacterBodyMotionProfile` descriptor、committed `WorldBodyState.VerticalVelocity`与TickDelta，以半隐式恒加速度积分生成唯一`CharacterMotionRequest`及同Step plan。正式World ResolveBatch Step Pass将同一内部SimulationTick的全部Actor request组成batch并交给Session唯一`ICharacterWorldSolver`；Solver只约束完整XYZ request并返回actual displacement、稳定Grounded与方向性Collision，Target唯一Finalize再提交actual Velocity与下一Tick VerticalVelocity，Program Finalize Pass最后生成Character state、body sample和motion fact。
- 当前 Unity 实现只在 `UnityCharacterControllerWorldSolver` 内调用 `CharacterController.Move`。Host、Kernel、Graph、Timeline、Action、Presentation 和 Network Model 都不能直接调用具体运动组件。
- 逻辑 body 属于 World state，visual root 属于 Presentation。不存在 `CharacterMotionStage`、`CharacterMotionAuthority`、`ExternalPose`、Motion Executor/Logic Pose Port 双路径或 Transform fallback。
- Standard Local Pipeline 不保存 rollback history。需要 prediction/reconciliation 或 rollback 的模型必须通过自己的 Source ports 及声明为 SnapshotParticipant 的 Pass 提供 typed ingress、restore、history/replay 和 EventId disposition，并使用匹配 NumericProfile/ABI/capability 的 Solver。Unity CharacterController与Deterministic KCC声明`AirborneVerticalMotion`；当前DotRecast Navigation Surface Solver不声明该能力，要求空中垂直动力的Program在Runtime创建前必须被Composition拒绝。
- Output disposition 同时携带 EventId 与 ActorId，Committer 按当前 batch 精确校验归属。需要 Replace、Retire 或 Suppress 历史的模型必须在自己的 SnapshotParticipant journal 中保存有界 EventId 历史，Unity 输出 aggregate 不保存无界 owner ledger。

### Presentation Direction

- Pose Graph、`CharacterAnimationPresentationProfile`、Rig v4、FullBodyIK Profile与Foot Analysis Source共同构成唯一动画authoring真相。Foot Analysis只发布root-local脚部事实，不拥有角色世界位移；Simulation唯一拥有实际平移。`CharacterFootPlacementProfile`保存Landing Prediction、Ground Path Reachability与Foot Motion正式配置，包含下一落点更新死区；预测点漂移不得降低Foot Goal权重；Rig v4唯一保存FBBIK biped映射。Runtime只消费精确Build产物。
- Program只提交有限Action的稳定producer、AnimationChannel、EventId、playback generation与committed raw sample锚点；Presentation从committed Body/Intent建立`CharacterPresentationFactFrame`，Projection独立保存PoseStateMachine、Projection-local dense source index与typed source plan、AnimationSlot、Transition Routing、Pose Plan、Policy与Rig payload。持续Locomotion不再要求Gameplay AnimationChannel，逻辑优先级、Tree site和Gameplay StateMachine transition不进入动画空间配置。
- 角色表现由唯一Factory装配Body、Animation、Action、FootPlacement与Camera runtime。FootPlacement在一个world-aware value operation内发布唯一Final Goal Set；`CharacterFinalIkFullBodySolver`随后只执行一次FBBIK。独立Grounding、Predictive Modifier、第二Goal链和图外骨骼写入都不合法。
- 角色表现运行链固定为 `SimulationCommitter / Model Egress -> 已注册的 CharacterSimulationPresentationRuntime -> Body -> Animation source capture / 单次Animancer Evaluate -> ordered Pose Plan stages -> 单次final writer -> Camera`。world-aware stage可出现在DAG中间，完成后后续pure-pose stage继续执行。`CharacterAnimationPresentationRuntime`在唯一Evaluate Barrier前完成identity、容量、source/release、Job和Final Writer binding验证；Barrier前失败按反向所有权顺序丢弃Pending页、journal与prepared resource，Committed状态和Physical Bones不变；Barrier内或之后失败直接标记Actor Animation Runtime Faulted并继续向上报告，不执行全量workspace、Registry或骨骼恢复。
- `CharacterPresentationRuntimeFactory` 是唯一运行时组合入口：本地 owner 使用 `CommittedStream + Direct + RequireCommittedSelection + Camera`，完整模拟的无相机 Actor 显式选择 `CommittedStream + Direct|BoundedCorrection + RequireCommittedSelection`，只观察外部状态的 Actor 使用 `SelectedStream + BoundedCorrection + AwaitCommittedSelection`。Source 与 Trajectory Profile 分开配置，不由相机、Actor 名称或 Network Model 推断。
- 唯一动画消费链是`committed Body/Intent -> Presentation Fact -> PoseStateMachine -> source capture -> Blend/Action/Local Pose -> LocalToComponentPose -> Component controls -> {FootPlacement | PoseBoneIKGoals} -> Final Goal Set -> 唯一FullBodyIK -> ComponentToLocalPose -> OutputPose`。Goal producer只输出typed value；FullBodyIK在独立Pending Component Pose中执行一次FinalIK FBBIK并重建受影响Virtual Bone。Runtime与Preview执行同一ordered Pose Plan，没有显式节点就不补建。
- Equipment Feature只声明所需producer集合，不拥有AnimationChannel、Player、Blend拓扑或第二份动画Profile；`CharacterEquipmentPresentationProfile -> CharacterPresentationProjection -> CharacterEquipmentVisualRuntime`只负责稳定VisualBinding、登记Renderer、Prefab/socket与local pose。同revision帧先应用装备外观再解析动画，外观失败只使Presentation进入明确invalid诊断，不反写Gameplay state。
- 正式Runtime与Preview复用相同的PoseState/source provider、有限Action lifecycle、Transition Routing、Animancer source backend、ordered Pose Plan、统一Foot Placement、typed Goal Set、FinalIK pose-buffer FBBIK backend和组合Debug View。Pose Graph Preview只提交Fact fixture；Timeline Preview只提交有限Action command；MM Query Fixture只提交query/relevance。Preview不直接`Play` Clip、不创建隐藏Gameplay producer，也不拥有第二套Virtual Bone、Grounder组件、shadow skeleton或IK solver。精确Host缺少Body或PhysicsScene上下文时，FootPlacement发布typed WorldContextUnavailable并阻断依赖该Goal Set的FullBodyIK，不构造假地面或默认Rig。
- Animancer只负责在同一PlayableGraph中复用和采样动画源；显式Player节点拥有各自source usage与连续性，局部Inertialization不保留旧source，Pose Graph Plan只在全部Goal Source、唯一FullBodyIK及后续stage精确完成后发布最终帧。Gameplay root motion只来自Program的显式motion operation，不能从动画结果反推。
- PresentationFrame按真实delta推进动画，Locomotion Pose与Step Fact使用同一Simulation Locomotion时钟。FootPlacement只读取同帧committed Body、原始Component Pose、原子Step Read Page、Locomotion Timeline、正式Future Body Translation、Profile、Rig、Calibration与本地PhysicsScene；当前Landing事务用Body Target世界速度预测平移，用当前Visible Rotation投影Raw Landing。工作区Runtime已经先在Current/Incoming中选择唯一事件再执行至多一次预测查询；每脚Landing事件由独立Pending/Committed生命周期持有，同事件Rejected不再清空最后Accepted，事件完成后才把它晋级为LastLanding。该结构仍属于active `complete-character-predictive-foot-ik`，尚未经过本轮端到端复验；FullBodyIK不查询、规划、锁脚或后处理。
- Camera 已闭合为正式 Presentation consumer。Cue、VFX 与 UI 当前只进入逐帧表现信号路由，尚无最终 consumer；Audio 尚无正式 Presentation command。后续补齐这些 consumer 时仍不得反向决定 Gameplay state。

### Shape Projection Rendering Direction

- Corin三渲二表现的唯一正式链是`Editor显式Bake -> 最终可见SkinnedMesh Pose捕获 -> Burst投影与Region Atlas布局 -> GPU逐Region二值Mask/Raw Depth/Completed Depth -> 只回读R8 Mask -> Burst闭环、共享边锚定与RDP -> 单Source一次Indirect Draw写Camera Color/Depth`。它的输入只有已完成动画、插值、IK和Secondary Motion后的Renderer变形结果、当前`RenderingData.cameraData`与不可变Profile/Artifact；输出只有角色Camera颜色和形状深度，不写Transform、骨骼、Gameplay、动画状态或网络状态。
- `CharacterShapeProjectionProfile`拥有颜色分区、微小区域合并、材质/子网格/Alpha规则、轮廓参数和固定容量；`CharacterShapeProjectionArtifact`拥有Mesh/Material/Texture lineage、稳定Renderer/Region/Shared Chain范围与Bake统计；`CharacterShapeProjectionSource`只拥有明确Renderer引用、显式形状投影总开关和当前Prefab实例参与关系。ProfileId与ArtifactId是资产级稳定身份，SourceId是每个运行时组件实例的稳定身份，不能序列化进Prefab后被多个角色实例复制。Runtime不按名称、层级、Tag、`Camera.main`或场景搜索恢复绑定。
- Source开关启用时，正式Renderer只保留`ShadowsOnly`的ShadowCaster职责，原PBR材质不再向Camera发布Forward彩色表面；开关关闭或组件禁用时，Source状态为`Disabled`并注销，Renderer恢复普通Forward彩色发布，且不创建Workspace、不提交GPU任务、不回读或合成。该切换只由作者显式操作，Waiting或Fault不自动恢复原始表面。Mask Pass位于不透明场景之后，Composite固定在`BeforeRenderingTransparents`，因此不透明场景先遮挡角色，角色发布的形状深度再参与透明VFX遮挡，现有全屏后处理最后消费已经完成的三渲二结果。Scene View、Preview、Reflection、XR和非Base Game Camera不提交该链。
- 每个Camera/Source使用固定三槽latest-only调度。slot满时跳过新提交并继续显示最近兼容结果，不同步等待、不扩队列；Camera Cut、FOV/Viewport或分辨率变化、Source generation、Profile/Artifact lineage和slot generation不一致会使旧结果失效。R8 Mask异步回读与CPU轮廓连接会带来一到数帧视觉结果年龄，但不会把延迟传回Gameplay、动画或Camera，也不会通过原始材质fallback掩盖等待或配置错误。
- Corin正式内容使用`Assets/Configs/Character/Corin/Rendering`下唯一Profile与Artifact，当前显式包含`Corin_Weapon`、`Corin_body`、`Corin_body_02`、`Corin_face`、`Corin_hair`五个opaque单材质slot；Artifact固定为27,916顶点、29,744三角、562 Region、582 Shared Chain，Region与Indirect容量为1024。Local、Rollback、两个Server Authoritative客户端和GameplayLab双角色显式绑定同一资产，AI从Local基座继承；authority-only对象不安装Source。
- 正常帧不聚类三角、不重建拓扑、不自动Bake、不回读深度，也不提供普通Outline、Sobel、CPU Rasterizer、同步Readback、低质量Mask、backend selector或原始PBR fallback。后续优化若改为全局Region ID、GPU轮廓/RDP、GPU Skinned Vertex Buffer或跨Source批处理，必须通过独立change替换同一阶段并保持Profile/Artifact/结果identity，不得并列保留第二实现路径。

### BTSMTL Authoring And Runtime Direction

- BTSMTL 是 authoring 基座，不是 Character Gameplay runtime。`BaseGraph`、Node、Edge、StateMachineGraph、TimelineData、TreeClip 和 `BaseExposedProperty` 继续承载编辑数据与稳定 authoring identity。
- Tree、AI、Character Pose Graph和Blend Space资产模式只使用唯一`GraphAuthoringEditorShell`，并由domain adapter装配Toolbar、左侧Navigator、唯一Graph Canvas、右侧Details和可选Bottom Dock。窗口尺寸、折叠、页签、Catalog筛选、Preview相机、Blend Space预览点和Pose Watch只属于window-local editor state，不进入Runtime资产。Tree/AI的Data与Details同时可见且不共享写状态；Pose Graph与Blend Space的Authoring、Live、References、Preview、Diagnostics、Sync和Pose Watch都只读或经各自唯一mutation owner写入。Compile、Program/Projection Build、Foot Analysis和Motion Matching Database Build只能由明确命令触发，selection、Inspector、窗口恢复和AssetDatabase refresh不得自动构建。UI可使用`Blend Space Player`、`Layered Blend Per Bone`、`Sync Group`、`Pose Watch`等UE对应显示名，但不得把AnimationChannel称为Montage Slot、把BTSMTL Timeline称为Montage、把Gameplay StateMachine称为Animation State Machine或把world-aware阶段称为独立Post Process Anim Blueprint。
- Character Runtime 不创建 `RunnableTree`、`StateMachineGraphRuntime`、`TimelineRunningTree` 或 TimelineData runtime clone。Compiler 将它们降低为 Program operation、control-flow edge、state slot、catalog、producer 和 source map。
- `StateMachineNode` 与 nested StateMachine 继续使用 inline/shared authoring ownership；Runtime 的 enter/update/exit、transition、interrupt、graceful stop 和 force stop 由 Program operation set 唯一实现。
- TimelineNode 默认拥有 inline TimelineData，显式共享时才引用 TimelineAsset。TreeClip 的 Decision/Commit、window variable、cue、motion 和 animation producer 都编译进同一 Program/Projection，不保留 scheduler 或独立 TreeClip runtime。Action Window 由 source State body 的 local Frame declaration 和 Decision TreeClip 写入，Transition 通过 `WindowType + current ActionInstanceId + current Tick` 查询同一 staged projection candidate；不保留按动作命名的 RootTree cancel key、窗口 registry 或历史 cache。
- Pipeline Blackboard 使用 `BaseExposedProperty` 作为唯一 authoring declaration。Character、Graph、State、ActionInstance、Frame scope 会编译成明确 state layout 和 owner address，不创建运行时局部字典服务或按名称 fallback。
- AI Blackboard使用独立Controller、Tick和Graph scope：Controller memory进入`AIControllerState`并跨Tick保存，Tick memory只存在于单次Evaluate workspace，Graph declaration保持局部owner identity。AI不得读取Character Blackboard或Character State；AI与Character之间唯一可写边界是最终`CharacterSimulationInput`。`CommittedActorObservationSnapshot`只公开锁定roster中上一轮committed Logic Body，Player ActionTarget与AI Perception共用该来源，不读取Actor registration缓存、Scene Transform、Tag、名称或当前Tick部分结果。
- ActionProfile 的 granted tags 以 `action:<ActionInstanceId>` source 进入唯一 Gameplay Effect Tag Container，并在 Complete、Cancel、Interrupt、Abort 或 teardown 时撤销。ActionProfile使用类型化`ActionTargetRequirement`声明`None`、`OptionalSnapshot`或`SnapshotRequired`；Transition 的 `CanActivateAction` 纯查询与最终 activation读取同一个显式目标候选并复用唯一portable admission evaluator。`OptionalSnapshot`有目标时固定快照并执行Warp，无目标时仍允许动作并保留源MotionCurve。ActionInstance只保存激活时的不可变目标快照，不按TargetId查询scene或Presentation。source State OnExit必须先结束旧ActionInstance，target activation不负责隐式取消source。
- Equipment authoring以`Character Root`、`Equipment Persistent Root`与`Equipment Route Root`组成显式composition root catalog，全部复用同一Graph discovery、control topology、Value Port与Timeline lowerer。通用Equipment Host只按committed Slot/Feature/revision调度预编译entry；换装事务在同一Character transaction内撤销旧Tag/Effect、替换revision/generation、重置Feature local state、安装新贡献并发布durable visual selection，不读取Feature资产或调用C# callback。
- Character Gameplay operation set 当前为 `/11`；`ActionWindowActive`、`CanActivateAction`、`TimelineMotionWarp`与通用Equipment identity/parameter/change/host/route operation都是numeric-neutral operation。MotionWarp portable descriptor保存Translation Mode、Target Offset Space、Rotation Mode/Method、Limit Policy及仅当前mode消费的curve/rate；Float32与Fixed target分别实现数值、曲线和typed累计轨迹state端口，并复用portable Action admission、Equipment transaction、Motion Modifier eligibility和lifecycle语义，不复制业务规则。当前Float32 Program ABI为7、Fixed Q32.32 Program ABI为6；`ActionTargetSnapshot`与不可变`EquipmentActionContext`进入同一Character State Transaction，两个Target的codec/hash/history保持相同业务字段顺序。
- Agent authoring 当前是editor-only的`canonical v3 package checkout -> AI用通用文件工具直接编辑editable/**/*.json -> Reconciler -> immutable AgentMutationPlan -> handler preflight/apply -> domain Validator -> canonical反向同步`。Unity资产保持正式真相，显式`.btsmtl/`目录包位于Unity项目根`AgentAuthoring/Documents/`且不进入`Assets/`；系统以整包计算`Clean`、`TreeDirty`、`DocumentDirty`、`Conflict`，不监听文件、不因selection/focus自动apply或build。MCP只提供`btsmtl.checkout_document`、`btsmtl.rebase_document`、`btsmtl.dry_run_document`、`btsmtl.apply_document`和`btsmtl.validate`五个生命周期工具，不提供局部图编辑工具。Graph JSON只表达stable kind、typed properties、logical port与system anchor；能力目录、Exporter与Reconciler共用`AgentAuthoringCapabilityCatalog`。`CharacterController` editable覆盖Graph、StateMachine、Blackboard、Timeline、Action、MotionWarp与Presentation正式业务字段；`AIController` editable覆盖Definition、Graph、Blackboard、Perception、Observation、Memory与Intent。Rig资源正文、Body Motion、Foot Analysis、受控Character合同、runtime state与generated product只进入context hash或完全省略。旧v1/v2、Intent、Macro、Patch JSON、operation外部catalog、bootstrap、action multiplexer和v15-v17兼容入口均已删除。
- Timeline Authoring Preview只预览有限Action Timeline，并与Pose Graph Fact Preview、MM Query Fixture共用唯一`AnimationPreviewRuntime`、Projection、source backend与Pose Plan；三者分别通过Action command、Fact和PoseState relevance/query fixture adapter提交输入。每个Action AnimationTrack在独立Timeline Editor中拥有Marker Sync子轨投影，`None`显示禁用摘要，`MarkerGroup`显示group、topology、SyncRole与marker，但子轨不进入`TimelineData.Tracks`也不执行Tick。Timeline Preview创建session-local ActionInstance并走Action inbox、Action Playback Input和AnimationSlot；Pose Graph/MM Preview不得创建Gameplay producer、AnimationChannel winner或PlaybackId。缺少Body/Physics world context时，FootPlacement阶段发布typed Unavailable，不伪造平面。单来源MotionCurve可按Timeline帧率从零绝对求值，并把累计位移与朝向只读投影到visual root，结束预览时恢复原姿态；该投影不产生MotionRequest，也不修改logic root、CharacterController或Simulation body。TreeClip、Action Cue、跨来源Motion仲裁与MotionWarp只显示和编辑，不执行Gameplay或WorldSolver。Live Debug只消费正式Timeline、Action lifecycle、PoseState source、Pose Plan与Marker relation snapshot，不从Animancer weight重建第二份事实。
- Corin Standalone当前注册玩家与`corin-training-enemy`两个同Session Actor。玩家使用显式Player control source、`LocalOwner`表现和绑定训练敌人的Session Actor target provider；训练敌人prefab已半迁移为AI control source与`SimulatedActor`表现，但仍引用Unity无法加载的旧Definition，因而当前不能作为已闭环AI运行。两者继续复用同一Corin Program、Projection、WorldSolver和Presenter。Corin Attack Profile为`OptionalSnapshot`，Attack1到Attack5主MotionCurve各有一条可调MotionWarp，统一关闭Translation并只用FaceTarget + ProgressCurve修正目标yaw，Dodge保持无目标；该样例尚不包含命中、伤害或受击闭环。

### Diagnostics Direction

- 调试链固定为 `Authoring identity -> Program source map -> operation/boundary Trace -> diagnostics sink -> RuntimeDebugSession -> Graph/Timeline/Host view`。
- Compiler、Pipeline preparation/compile、Ingress/Schedule/Step/Egress、restore/world batch/state publish/commit、Solver 与 Presentation 分别在自己的边界发布结构化 Trace。
- Trace 保存 source clock、SimulationTick、Actor/Program、operation/state slot、CharacterStateHash、WorldHash validity、Solver capability、snapshot identity 和输出 disposition。
- Diagnostics 是只读观察面，不持有 runtime clone、不暴露 mutable state、不改变 Source/Pipeline policy、World solve、output disposition 或表现结果。

### Network Boundary

- Simulation Core 只声明 Program、Input、State、Pipeline descriptor/product/plan/snapshot、Source port、World request/result、Fact、Solver、Commit 与 outer runtime handle 合同；不引用 packet、endpoint、transport、具体 Network Model policy 或 Unity API。
- Network Model 通过自己的 Session Source、phase-specific Pass 和 Pipeline Definition 将 packet 降低为 typed product、restore/replay plan 与 EventId disposition。Character、Graph、Timeline、Action 和 GameplayEffect 不选择模型，也不保存模型策略。
  - 当前可运行Gameplay组合包括Local Unity CC、ServerAuthoritative Prediction Unity CC、ServerAuthoritative Authority Unity CC，以及隔离的DeterministicRollback FixedQ32.32 + Deterministic KCC对比Demo。DotRecast Prediction/Authority产品边界仍保留为Float32 Navigation Surface后端，但当前Solver不支持`AirborneVerticalMotion`，因此不能与已要求该能力的Corin Program组成Active Session；后续必须补正式空中World backend，不能丢弃Y、假Grounded或关闭Body Motion绕过。ServerAuthoritative仍是作品主线；Rollback Demo由一个纯.NET Dedicated Relay Server和两个Unity Peer进程组成，Relay只负责输入立即转发、canonical排序、confirmation与hash/snapshot路由，不执行Program、KCC、WorldState或Presentation。Rollback只复用公共authoring、Semantic IR、SessionHost与Actor registration合同，不复用Float32 Program、Authority correction或DotRecast world execution。每个可运行ServerAuthoritative测试环境的Prediction与Authority使用相同ProgramHash/LayoutHash、显式兼容的不同PipelineHash和相同Solver/World identity；具体Solver只由Composition或Authority Scene manifest选择，旧LocalLoopback不存在，也不得重新引入。
- ServerAuthoritative Prediction通过唯一`ServerAuthoritativePredictionState` aggregate root协调内部Confirmation/Request、History、Disposition Journal与无状态Reconciler；三个Pass不得直接持有子模块或复制cursor。History模块拥有唯一Remote Body timeline、每Tick owner input、完整committed world snapshot、Pipeline state projection、已选择观察frame和journal cursor；Correction Schedule是唯一restore/replay/current ExecutionPlan与Remote Body tick选择producer，Replay必须复用History保存的精确观察frame。Output Disposition是唯一EventId journal owner。Ack、baseline与restore先生成完整候选checkpoint和restore plan，再提交模块状态；Correction v4、History v3与Journal v2是当前Participant identity和canonical schema，Body恢复状态包含独立VerticalVelocity。
- Fantasy Gate/Room只拥有控制连接、固定双人 roster、identity校验、一次性data-plane ticket、可靠Event/Full Checkpoint精确路由和Session失败传播。Portable ServerAuthoritative模块唯一拥有Authority Pipeline catalog、每Actor command queue、authority clock、每Client checkpoint baseline、snapshot sequence、reliable/full-checkpoint output queue与direct UDP数据面；Unity Authority Worker和Fantasy DotRecast Authority Scene只提供各自Host adapter、显式Solver与lifecycle装配，并通过`ServerAuthoritativeAuthorityHostLaunchRequest`调用唯一Float32 Composer。Gate Scene不读取gameplay datagram或Character state；DotRecast Authority Scene不引用Unity。
- Unity authoritative process、DotRecast/C# authoritative server 和 deterministic rollback 是不同完整组合，可以共享同一 authoring、Semantic IR、portable descriptor 与公共 Host，但必须各自提供匹配的 Program Runtime、Backend、Source、Pipeline 和 WorldSolver，不能同场双算或互为 fallback。
  - FixedQ32.32 只属于已安装的 DeterministicRollback target，不是公共 Gameplay Core 的强制数值格式。Fixed backend复用同一 versioned operation set与 portable control runtime，并通过独立 Fixed Program/State/Kernel ABI执行 numeric/domain leaf；项目不得新增 DeterministicMoveNode、RollbackTimelineRuntime 或第二套业务 evaluator。
- DeterministicRollback 的 Fixed KCC 与 DotRecast Float32 Solver 只共享 `SolidBodyBlock` 业务语义，不共享 contact implementation、request ABI、history、runtime 或 world state。Rollback Fixed KCC只快照下一Tick会读取的grounded与stable support primitive/feature/normal；query candidate、contact manifold、ledge/step诊断和iteration统计不进入Snapshot或Hash。Rollback batch 中两个Actor都是Fixed Active body；ServerAuthoritative客户端的remote actor不运行Program、不拥有Character state。Prediction Schedule从唯一Remote Body timeline选择其Body，声明`ObservedKinematicActorContact`能力的Composition把该选择作为只读观察约束送入唯一WorldSolver，未声明能力的Composition提交正式空观察frame；同一selected Body流驱动Remote Presentation。
- DotRecast静态查询只负责Navigation Surface，完整`DotRecastWorldSolver.ResolveBatch`再通过唯一portable `ActorContactSolver`裁决同batch Actor硬接触。Authority的active/active保持对称裁剪；Prediction的active/observed使用相对扫掠且只修正active一侧，observed不产生Result、不进入NextWorldState。正式顺序固定为全部Surface candidate、垂直区间过滤、稳定pair连续圆盘扫掠、闭合法向裁剪、Surface重新约束、最终间距校验和原子FinalBody；Network Model、Authority Scene、Presentation与场景Collider不执行第二次碰撞。
- 共享 DotRecast Navigation Solver 使用同一份 portable adapter、`ActorContactSolver`和exact-byte `NavigationSurfaceArtifact`同时服务Unity Prediction与普通.NET Authority Host；它只接受显式contact shape的state-only world body binding，禁止引用`CharacterController`、`Rigidbody`、Scene Transform真值、Runtime烘焙或跨Tick polygon/contact cache。Unity CharacterController Solver仅属于既有Unity Authority对照组合。
- 当前 DotRecast Float32 Solver 与 Rollback Fixed KCC 分别实现同一种运动学 `SolidBodyBlock` 业务规则：静止 Actor 不会被主动撞入者转换成推行位移，双方运动时裁剪闭合法向并保留切向滑动。两者不共享实现，也都不提供攻击推人、击退、霸体、ghost、队伍穿透、动态障碍、刚体或RVO避障；这些业务以后必须通过各 Target 正式 Motion/World request 进入对应 Solver，不能按 Action、Tag 或动画 producer 硬编码。
- `NavigationGeometryArtifact -> NavigationBuildTool -> NavigationSurfaceArtifact -> NavigationSurfaceAsset -> DotRecastWorldSolverDefinition` 是唯一导航烘焙与加载链。业务 Ground area `0` 只在 Detour polygon catalog 中使用，Recast 构建阶段必须使用其内部 Walkable area，不能混用编号。
- 命中、伤害、目标归属、跨角色 GameplayResult 和局部 combat rewind 尚未闭环，不允许用 Character 脚本或 GE 直连碰撞绕过 Session/World 边界。

### Network Model Test Environments

- 网络模型使用相互隔离的测试 Scene。Bootstrap Scene 只按显式 TestScenarioId 跳转到目标 Scene，不创建 Program Runtime、Backend、Pipeline、Source、Solver、Endpoint、Actor registration 或 Session。
- ServerAuthoritative Player启动必须同时提供 `--network-test-scenario=server-authoritative-client|unity-authority-worker|dotrecast-authority-client` 与匹配的 `--server-authoritative-role=authority|client-a|client-b`；场景与角色、Launch锁定的HostProfile不匹配时直接拒绝启动。
- Local、ServerAuthoritative Client、Unity Authority Worker、DotRecast Client 与 DeterministicRollback Peer 分别使用明确的测试 Scene/launch profile。Client A/B 或 Rollback Peer A/B 可以复用同一角色 Scene，但必须由不同的显式 process role/player identity 启动，不能按连接顺序、对象名或默认值猜测身份。
- 每个角色 Scene 显式引用完整 Session Composition、Actor/出生点、World binding、Endpoint/launch profile、Camera 与 diagnostics。Scene 不内联五项组合规则，也不通过运行时下拉、enum switch、场景搜索或 fallback 改写活动 Session。
- 三个Network Test Product使用schema v2 manifest的`NetworkModelIdentity + RuntimeTopologyIdentity + artifacts[]`表达运行闭包。Unity Authority为Fantasy Gate Server加Unity Authority Worker与两个Client的四进程产品，DotRecast Authority为包含DotRecast Authority Scene的Fantasy Server加两个Client的三进程产品，DeterministicRollback为纯.NET Relay Server加两个Unity Peer的三进程产品；公共Build Workflow不认识具体Network Model或Server类型。
- DeterministicRollback Player Build必须通过显式Fixed-only Build Request从当前Character Definition只运行一次Frontend，并由Projection Compiler与Fixed Target Adapter从同一validated Semantic IR独立生成Presentation Projection和Fixed Program；ProgramId、SourceRevision、SemanticHash、ContractHash或ordered producer contract不一致时必须在Player Build前失败，禁止生成Float32前置产物或静默打包旧图产物。
- DeterministicRollback Peer Scene复用`Assets/Scenes/Shared/CharacterMovementTestEnvironment.prefab`，但运行时只读取Build前由该Scene显式World/Surface authoring烘焙出的Fixed Collision Artifact；可见Collider与artifact作者数据不得分裂。
- 场景切换必须先销毁旧 `SimulationSessionHost`、Actor registration、Endpoint 和模型资源，再在新 Scene 创建新 Session。Session、Actor、Solver 与网络队列不得通过 `DontDestroyOnLoad` 跨测试 Scene 存活。
- Unity Authority Worker使用独立Unity Scene和独立OS进程；DotRecast Authority运行在Fantasy Server内的独立MultiThread Authority Scene，不是额外OS进程。对应客户端Scene只引用匹配的Composition、endpoint、launch profile与地图identity。
- Bootstrap 跳转是测试环境选择，不是 Active Session 热切换。返回 Bootstrap 后再次进入其它模型时必须形成新的 composition identity 和完整 lifecycle。

## Code Organization

- `Assets/GameScripts/Main/Runtime/Simulation/Core`：portable Character与AI Semantic IR schema/canonical codec、跨Target identity、operation-set、Equipment identity/catalog/state/transaction合同、Pipeline descriptor/product/plan/state合同、outer runtime handle、不可变`OperationExecutionTopology`、唯一portable Runnable/StateMachine control、Timeline control、GameplayEffect准入/生命周期、Pipeline Transaction coordinator与workspace合同；不引用Float32、Fixed具体ABI、Unity、Presentation或具体Host Product。
- `Assets/GameScripts/Main/Runtime/Simulation/Core/Float32`：Float32 Character与AI Program、State、Kernel、Snapshot、标准 Step Pass、Pipeline transaction/backend/runtime handle 与执行实现；Character仍由`Float32OperationEvaluator`作为唯一Actor/Tick operation入口，AI Intent只在唯一Control Input Ingress前生成普通Character input。Program composition建立结构化Value input span、typed catalog字段、Action profile、Timeline/State owner、named constant和state address索引；领域模块只消费validated Program layout与当前transaction state，不读取authoring或在Tick内重建静态关系。
- `Assets/GameScripts/Main/Runtime/Simulation/Core/Fixed`：Fixed Q32.32 Program、State、Kernel、Snapshot、标准Step Pass与执行实现；与Float32共享同一Semantic Operation Set和portable control合同，但拥有独立ABI、ProgramHash、LayoutHash、State codec与Target-specific Program layout。
- `Assets/GameScripts/Main/Runtime/Simulation/Unity`：由`ThirdPersonSimulation.Unity`唯一拥有五项Composition Definition、Local Source与Pipeline authoring、Float32 Composer/Backend、CharacterController WorldSolver、body binding和Unity数值边界；不得引用Character Presentation、Timeline Authoring Preview、具体Network Model、Fantasy或DotRecast Unity实现。Timeline Authoring Preview只属于Character Presentation并且不创建Simulation Session。
- `Assets/GameScripts/Main/Runtime/Simulation/Unity/DotRecast`：由`ThirdPersonSimulation.DotRecast.Unity`唯一拥有Navigation Surface资产、state-only body binding与DotRecast Solver Definition；portable查询和artifact继续属于`ThirdPersonSimulation.DotRecast`。
- `Assets/GameScripts/Main/Runtime/Simulation/DotRecast`：portable Navigation artifact codec、显式contact shape的state-only binding descriptor、共享query solver、ActorContactSolver与结构化查询/接触诊断；不引用Unity、CharacterController、Fantasy、Graph、Action、Presentation、Recast build API、Crowd或TileCache。
- `Assets/GameScripts/Main/Runtime/Simulation/DeterministicKcc`：Fixed Collision World query、Fixed contact shape codec、stable ActorId pair sweep、`SolidBodyBlock`、静态重约束与结构化接触诊断；只引用 Core 与 Fixed Target，不引用 DotRecast、Unity、Network Model、Graph、Action 或 Presentation。
- `Assets/GameScripts/Main/Editor/CharacterSimulation`：多composition root Authoring Discovery、Semantic Emitter、Frontend、`.csir` store/Inspector、target-neutral Projection Compiler与Semantic Reader、Float32/Fixed Target Build Adapter、Semantic IR/Program/Projection原子发布组、Pipeline/Composition Inspector，以及唯一Network Test Product Build Workflow与三个显式Product adapter。目录按`Compilation/{Semantic,Presentation,Programs}`、`Build`、`Analysis/FootPlacement`、`Navigation`、`NetworkProducts/{Shared,DotRecast,UnityAuthority,DeterministicRollback}`与`Inspectors`聚合；Fixed Compiler继续保留独立程序集边界。公共workflow和utility不依赖任一具体Product adapter，Projection Compiler与Numeric Target Adapter互不调用。
- `Tools/ThirdPersonSimulation.Portable/ThirdPersonSimulation.NavigationBuildTool`：唯一 Recast build-only 入口；读取 canonical `.navgeom` 并输出 canonical `.navsurface`，Player 与普通 authority runtime 不引用 Recast builder。
- `Tools/ThirdPersonSimulation.Portable/ThirdPersonSimulation.Reader`：受版本控制的普通.NET artifact检查入口；显式读取`.csir`、Float32 `.csim`与Fixed `.fixed-program`，输出identity、table summary和结构化Value inputs，不引用Unity、反射authoring或重建缺失binding。
- `Assets/GameScripts/Main/Runtime/BTSMTL`：Graph、Tree、Node、Edge、ExposedProperty、Timeline authoring 与通用编辑器/诊断基础。
- `Assets/GameScripts/Main/Runtime/Character`：由`ThirdPersonClient.Runtime`唯一拥有Character、Equipment authoring、Camera、Animation、Presentation、`SimulationSessionHost`、Actor registration与隔离Preview host；只通过公共Composition入口启动Session，不反向引用具体Network Model程序集。Equipment Profile只用于Editor compile/Projection build，Gameplay runtime不读取Feature或Equipment Unity资产。
- `Assets/GameScripts/Main/Runtime/Character/AI`与`Assets/GameScripts/Main/Editor/AI`：分别拥有Unity AI Control Source/Definition/节点/Program wrapper，以及AI薄窗口、Inspector Context和exact-byte Compiler；不拥有第二GraphView、Character执行器、Scene Bot脚本或网络AI路径。
- `Assets/GameScripts/Main/Editor`：由`ThirdPersonClient.Editor`唯一拥有Character、Simulation、Agent、Inspector与Exporter编辑器代码，显式引用正式Runtime程序集，不再借用`Assembly-CSharp-Editor`。
- `Assets/GameScripts/Main/Editor/CharacterPipeline`：共享Character authoring按`Authoring/{ActionWorkspace,SharedGraph,Animation,Definition,PoseGraph}`、`Inspectors`、`Analysis/FootPlacement`、`Diagnostics`、`AgentAuthoring`、`MotionMatching`与`RootMotion`聚合；根目录不保存散落实现，不增加第二GraphView、第二Mutation或第二Compiler入口。
- `Assets/GameScripts/Main/Editor/ProductBuild`：唯一商业客户端构建入口、固定产物 layout、Content/Player staging、StartupPolicy writer、release manifest、hash/exact closure 校验与原子发布；TEngine Editor 只执行显式请求的 HotFix、YooAsset 和 Unity Player 底层构建，不拥有项目版本与正式目录。
- `Assets/GameScripts/Main/Runtime/Character/Pipeline/Animation`：Animation Presentation Profile、Rig v4与Projection ABI、PoseState/source provider、Action Slot、Transition Routing、Physical/Virtual source capture、SelectedPosePlayer、BlendSpacePlayer、显式BlendStack、局部Inertialization、显式Local/Component Pose转换、Component Pose骨骼控制、typed FullBodyIK Goal合同、FinalIK pose-buffer FBBIK solver、ordered staged Pose Plan、native job、workspace、source-map与统一诊断合同；`Contracts`继续属于同一Runtime程序集，但按`Action`、`Blend`、`Rig`、`Pose`、`Sources`、`Projection`、`Workspace`与`Common`聚合，不再作为平铺杂物箱。不存在通用Pose port、固定per-slot Stack、第二运行链、Stack Inertial owner、图外Virtual Bone缓存、手臂IK pass、TwoBoneIK/LegIK operation或固定world-aware尾阶段。其`MotionMatching`子目录拥有target-neutral MM payload、trajectory/query/search/plan/pose source与只读诊断合同，不读取Gameplay state、Network Model或Editor Artifact。
- `Assets/GameScripts/Main/Runtime/Character/Pipeline/Presentation`：唯一Presentation Factory/协调器、Body source cursor、target轨迹采样、通用Visual Trajectory Follower、角色级Body Presentation Profile、`CharacterAnimationPresentationRuntime`、有限Action专用`CharacterActionPlaybackRuntime`、Animancer source backend、统一`CharacterFootPlacementGoalSource`、通用World-Aware Binding、可选Camera runtime与Cue路由。FootPlacement只由一个Pose Plan value operation调用并发布typed Final Goal Set；FullBodyIK只消费Component Pose与Goals。不存在独立Predictive Modifier、GrounderFBBIK组件、shadow skeleton、图外composition、LegIK或第二骨骼写入。
- `Assets/GameScripts/Main/Runtime/Character/Pipeline/Diagnostics`：Simulation/Presentation Trace 到 RuntimeDebugSession 的只读 adapter。
- `Assets/Configs/Character/<Character>/Pipeline`：角色authoring按`Actions`、`Behavior`、`Definition`、`GameplayEffect`、`Graphs`、`Input`、`Motion`、`Presentation`、`Simulation`与排队中的`AI`聚合。Corin Presentation进一步固定为`Profiles`、`Blend`、`Rig`、`FootPlacement`、`PoseGraphs`与`Sources/Actions`；Pose持续源只存在于Pose Graph/Profile子资产，不再创建顶层Locomotion Transition Asset。
- `Assets/Configs/Simulation/GameplayLab`与`Assets/Configs/Simulation/DeterministicRollback`：分别按`Compositions/Pipelines/Sources/Variants`与`Compositions/Pipelines/Programs/World/Networking`聚合。Runtime Profile Prefab按`Local`、`AI`、`Rollback`与`ServerAuthoritative/{UnityAuthority,DotRecast}`聚合；路径只由正式Collector、Launcher和Product adapter显式引用。
- `Assets/GameScripts/Main/Runtime/Networking/GameplayNetwork`：由`ThirdPersonGameplay.NetworkModel.Unity`唯一拥有model-neutral `GameplayNetworkModelDefinition`、Session Source基类和preparation requirement；不拥有公共Host、Pipeline compiler、Kernel、Solver或具体模型类型。
- `Assets/GameScripts/Main/Runtime/Networking/GameplayNetwork/ServerAuthoritative`：由`ThirdPersonSimulation.ServerAuthoritative.Unity`唯一拥有Fantasy Endpoint、Prediction/Authority Source preparation、host-neutral control transport的Unity adapter、launch identity、remote presentation registration与隔离测试Scene入口；不拥有Authority queue/clock、Pipeline catalog，也不执行Program或WorldSolver。
- `Assets/Generated/NetworkProtocol`：由`ThirdPersonGameplay.FantasyProtocol`唯一拥有Fantasy生成协议类型；协议仍只通过正式ProtocolExportTool生成，模型Unity程序集显式引用该生成程序集。
- `Assets/GameScripts/Main/Runtime/Simulation/Core/Float32/Network/ServerAuthoritative`：模型产品、canonical codec、Prediction history/correction/output disposition、Authority Pipeline catalog、Source policy/runtime、control/data transport合同、Host launch request及 Authority input schedule/replication Pass；不引用 Fantasy message或 Unity对象。
- `Assets/GameScripts/HotFix`：TEngine 热更程序集目录。
- `Assets/GameScripts/Main/Runtime/ProductStartup`：AOT 启动合同、ProductStartupProfile、StartupPolicy、只读 snapshot、generation/cancellation runner、资源初始化 adapter 与内置 Bootstrap view；只拥有 HotFix 可用前的启动状态。
- `Assets/GameScripts/HotFix/GameLogic/ProductStartup`：唯一 ProductStartupCoordinator、ProductShell 登录/Home/Gameplay 子状态和场景进入；不创建第二 Gameplay runtime。
- `Assets/GameScripts/HotFix/GameLogic/ProductResource`：项目层 Global/Home/Gameplay/Transient ResourceScope、logical lease、in-flight physical load 合并、业务 PreloadPlan 与安全点回收。
- `Assets/GameScripts/HotFix/GameLogic/ProductDiagnostics`：启动、资源、认证和公开 Unity 内存指标的只读 snapshot，以及仅 Editor/Development Build 的 Fault Lab 边界。
- `3cDemo/Server`：Fantasy Gate Scene拥有Room、generated Outer/Inner协议、固定roster、Host route和控制路由；DotRecast Authority Scene通过portable manifest/runtime launcher拥有该Host的Program、Source、Pipeline、Solver、World和UDP端点；Startup AuthGateway独立拥有游客认证Registry。Gate不拥有Gameplay state，Authority Scene不拥有Client控制Session或Presentation，Startup不引用Authority与Gameplay portable runtime。

## Conventions

- 生成代码尽量少写注释，只在关键复杂边界写少量注释。
- 不做 fallback 配置、兼容镜像、临时桥接路径或双主线。
- 旧数据、旧路径、旧命名确认不用就直接删除。
- 修改代码不用 MCP 写文件；Unity MCP 只用于刷新、查看状态和 Console。
- 永远不要运行 Unity batchmode。
- 文档读取必须显式 UTF-8。
- 默认不新增测试，除非用户明确要求。
- 用户负责 Unity 端到端验收；不要把手动验证写进 OpenSpec task。
- 绿色 CI 只表示仓库策略、OpenSpec 严格校验和 portable 单元测试通过，不表示 Unity Editor import、Unity Test Framework、Player build 或 Gameplay 实机测试通过。
- 当前没有 CD、Unity 云端构建、EditMode/PlayMode、AI 审查或 AI 实机测试；需要这些能力时单独建立正式 change，不保留占位配置。

## Cleanup Rules

- 旧 Workbench、旧 locomotion/action/footphase/bodyclaim 数据源不恢复。
- Character Gameplay 不恢复对象解释器、runtime Graph/Timeline clone、stage 私有状态、ExternalPose 或 MotionStage correction。
- Program/Projection 迁移不保留 runtime compile、stale artifact fallback、默认 Solver、Transform 搜索或双写资产。
- `Ref` 中代码只能迁入正式模块后改名归属，不能作为运行时依赖。
- archive 只作历史追溯，不作为当前实现目标。

## Pending Work

- `add-animation-transition-routing-module`已经在独立`ThirdPersonCharacter.Animation.TransitionRouting`程序集实现exact rule compiler、Standard Blend/Inertialization route decision、独立capture/release completion outcome、typed request、generation、握手、reset和有界snapshot。`refactor-animation-control-boundaries`已经把它编译进PoseStateMachine Transition与AnimationSlot的明确owner：模块只提供route decision和capture/release协议，不拥有PoseState、Slot、Player、BlendStack、Inertialization、PlayableGraph或最终Pose。Corin作者资产与generated Projection已经完成正式迁移和显式Build，旧`integrate-animation-transition-routing-pipeline`依赖BaseLocomotion Selection旧拓扑，已由当前职责change吸收并删除。
- 当前没有正式命中 solver、目标 registry、跨角色 GameplayResult 与完整 combat closure；不得由 Character、GE 或网络 Handler 旁路补齐。
- GameplayCue 尚缺统一 VFX/Audio consumer；动态 `List<PropertyPort>` UI 仍需收口。
- Agent Document当前运行实现是唯一v3目录包，Character Presentation通过共享Capability与typed Presentation Mutation进入editable，v2 reader、writer、manifest识别和迁移入口均已删除。`add-corin-training-ai-demo`可以在此基础上生成正式Definition、Tree、Perception、Program与prefab配置；AIController package只读引用受控Character capability，不复制Character Presentation editable。网络Authority AI仍需独立后续change，不得由Local客户端推断Authority Bot输入。
- Equipment Core后续仍需三个独立change：将Corin锯刃业务迁移到正式Slot/Route/Feature与Timeline；把Equipment editable/context接入Agent Document v3、Reconciler、validator及`btsmtl-agent-authoring`技能；为各Network Model定义Equipment request、canonical state/observation与远端visual selection。三者不得复制Equipment catalog、Action admission、Tag/GE aggregate或外观运行时。
- 商业启动链仍需配置唯一HTTPS ResourceEndpoint、WSS AuthEndpoint和正式共享字体、材质、图集打包规则；完成前商业启动代码只能视为未闭合实现，不能宣称Published Player已经具备可发布认证与资源交付闭包。当前没有负责这些剩余工作的active change，继续实施前必须建立或恢复唯一正式change。
- Local Fixed Gameplay Lab入口、场景、两个显式Variant与Fixed Host已经闭合；两个Variant引用同一Corin Fixed Program、Projection、KCC配置、可见环境与collision artifact，只在Session Source与Network Model装配上不同。Rollback Network Test Product已经通过唯一workflow发布IL2CPP Player与纯.NET Relay Server，Run继续只消费正式manifest。Gameplay Lab不是DeterministicRollback的第二装配真相。
- 动画代码正式阶段为`Presentation Fact -> PoseStateMachine -> state-local provider -> AnimationSlot -> Local Pose stages -> LocalToComponentPose -> Component Pose controls -> {FootPlacement | PoseBoneIKGoals} -> component.full-body-ik-goals -> 唯一FullBodyIK -> ComponentToLocalPose -> OutputPose -> FinalAnimationPoseFrame`。FootPlacement是Goal producer而非Pose solver；每帧最多一次Foot Placement事务、一次FBBIK和一次final writer。独立Predictive Modifier、旧FootGrounding作者节点、TwoBoneIK、LegIK和图外FinalIK组件都不属于正式链。
- Corin Locomotion Pose选择只消费Gameplay提交的`presentation.movement-mode`离散Identity，不再用速度或Facing Error重建第二状态源。MovingTurn的Gameplay入口只属于RunLoop在`MovingTurnAngleThreshold=135°`时建立的唯一业务边；RunEnd重新收到输入时先回到规范RunLoop，再由同一门禁决定是否进入Turn，不复制第二套条件。其60Hz有限Timeline保留源Root Motion前28帧，29个采样贡献直接使用`Animator.deltaPosition`得到的Unity米制值，累计X/Z为`(-0.9001478, 0.4623734)`、yaw为固定180°，不得再乘`0.01`或按目标角缩放，并以`state_root_completed`作为唯一释放门禁。Pose Graph只播放Turn Sequence，Gameplay输入转向与`RootOrientationWarp`不再形成第二朝向路径；Presentation允许RunStart、RunEnd在观察到已提交Turn事实时进入同一Turn Pose，以吸收逻辑提交与表现观察的帧差，但不创建Gameplay入口。RunStart、RunLoop、RunEnd进入Turn使用0.12秒Inertialization，Turn退出到RunLoop、WalkLoop或Idle使用0.30秒；Idle、WalkLoop与RunLoop保留连续播放相位，有限状态进入时重置。完成后按`HasDirectionalDodgeRunIntent`进入RunLoop或WalkLoop，停止输入则进入WalkEnd。持续Locomotion Sequence使用Simulation Locomotion时钟，使Pose、Foot Step Fact与Clearance保持同相位；有限Action继续使用自身正式Timeline/Marker时钟。Pose、节点和Graph运行内存不进入Rollback snapshot或网络协议。Action Timeline的Root Motion与MotionWarp保持不变。现有Native Pose算法不重写；Local Fixed双Actor与DeterministicRollback Relay + Peer A/B继续使用同一Program、Projection、KCC和Collision身份。
- Foot Placement代码目标链为`Biomechanical Step Read Page + committed Body Target世界速度 + Timeline段边界/Continuation -> KCC Future Body Translation -> 当前Visible Rotation投影Raw Landing -> 每有效表现帧唯一Future Landing SphereCast -> Accepted/Rejected Landing -> Ground Path Reachability -> Ground Envelope -> Foot/Pelvis Goal -> 唯一FinalIK FBBIK -> final writer物理骨骼写入诊断`。Artifact不发布角色世界位移，Simulation/KCC唯一发布Body Position、Facing、Linear Velocity与Angular Velocity；Landing不把输入方向、速度方向或瞬时Angular Velocity外推成未来Facing。代码存在不等于运行结果已经确认。
- 正式链不使用旧Predictive、Current Grounding、Anchor/Pelvis或自动Foot IK控制，也不得用响应式结果、固定高度、默认地面、旋转旧命中或其它求解器补洞。当前用户确认只到Accepted Landing、落点身份与Ground Path线；Ground Envelope、Swing Foot Motion、Pelvis、Locked/Sliding/Unlocked、脚掌朝向、Pivot、FullBodyIK消费和Physical Bone写入全部保持未确认。编译成功、Goal非零或CSV残差为零都不能替代对应阶段的可视化与同Completion证据。
- 第一阶段已经否决四角Sole Envelope查询：鞋底矩形属于最终支撑可视化，不是参考文章与GDC的Foot Path查询形状。每脚每个有效表现帧只允许一次向下SphereCast生成当前未来落点；Ground Path再按落点更新死区决定是否重建Capsule路径。Capsule Path之后的Ground Envelope、Swing、支撑、Pelvis与骨骼消费即使已有代码，也必须继续按`complete-character-predictive-foot-ik`逐阶段复验，不得提前描述为已生效。
- `reimplement-deterministic-kcc-from-philippe-motor`已经完成portable源码、正式KCC配置、state codec、Solver identity、current spec迁移与逐Collider SurfaceId；Philippe St-Amand Kinematic Character Controller是movement policy主要基准，Fixed query、collision artifact、rollback与batch ownership继续由项目实现。连续Low、High与OverLimit楼梯已经由后续`separate-stair-gameplay-and-foot-surfaces`迁移为六条Gameplay Ramp，因此旧逐级楼梯`Y=1.69/1.93/0.01`重放只属于历史artifact，不再作为当前连续楼梯表现验收；0.14m、0.24m与0.40m真实Step边界由独立`StepCapabilityCourse`继续表达。旧KCC算法和失效任务引用已经删除，不保留第二实现路径；KCC继续只拥有Fixed Gameplay状态，不进入Foot Placement、动画或Timeline事务。剩余端到端验收须按Local Fixed、Rollback Relay与Peer A/B顺序进行，完成前不得把历史产品日志当作新Collision身份的闭包证据，也不得自动构建、发布或运行时修复。
- Pose Graph必须通过Pose IR编译为Presentation Projection与Native Pose Program，但Pose、节点实例和Graph运行内存不进入Rollback snapshot或网络协议。Local Float32、Local Fixed与DeterministicRollback继续由显式Variant选择Simulation Program与Session/Network Model；同步方式属于Variant装配，不能由Pose节点、作者UI或资产迁移器暗中决定。

Character Build、资源重建和编译属于显式重操作：任务要求“实施、闭环、本地测试”或用户已经授权全流程时，由Agent通过精确Definition/目标主动触发；禁止选中资产触发、运行时自动补建、无目标全量构建或把旧产品当成新实现。
