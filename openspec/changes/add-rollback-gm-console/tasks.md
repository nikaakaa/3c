## 1. 正式合同与服务端装配

- [ ] 1.1 根据用户确认的服务宿主和工具连接方案补齐精确产品、依赖、endpoint、访问配置与 Build/Run delta；该项未确定前不实施相关宿主代码。
- [ ] 1.2 定义命令描述、参数、请求/结果、请求关联、服务/会话运行身份和明确错误状态。
- [ ] 1.3 实现服务端显式命令注册、开发权限及参数校验和处理器分发，不引入任意脚本执行或业务 switch。
- [ ] 1.4 提供 Relay 正式只读查询端口和快照边界，区分预期 roster 与实际运行事实，不镜像可变状态。
- [ ] 1.5 独立实现并注册 `help`、`session.info`、`actor.list`、`runtime.status`，返回真实服务端结果。
- [ ] 1.6 接通选定的工具请求/响应、容量、超时、断线结果与操作日志，不使用玩法同步协议或本地 fallback。

## 2. 客户端控制台

- [ ] 2.1 实现文本命令与参数解析、请求提交和结构化结果展示，不在 UI 编写命令业务逻辑。
- [ ] 2.2 完成打开/关闭、连接身份、命令历史、有界输出和本地清屏，禁止重绘回调执行重操作。
- [ ] 2.3 在正式设备适配边界接入控制台焦点，防止输入穿透，不清理历史或已提交请求，不暂停 Rollback。
- [ ] 2.4 显式装配到 Rollback Development 入口，拒绝正式非开发产品误装，不顺带接入其它模型。

## 3. 产品闭合与文档

- [ ] 3.1 依据选定宿主完成正式服务与客户端配置、manifest、构建和启动闭包，不在 Run 阶段临时生成或修复产品。
- [ ] 3.2 同步最终已安装规格、命令用法和扩展入口说明；保持玩法 GM、采样、诊断与 Action 修复在本轮范围外。
