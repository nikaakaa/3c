## 1. 合同与独立服务

- [x] 1.1 按用户确认的独立进程文本控制台补齐工具协议、认证、产物和四进程 delta。
- [x] 1.2 定义命令描述、参数、请求/结果、请求关联、运行身份和明确错误状态。
- [x] 1.3 实现显式注册、权限和参数校验、异步处理器分发，不执行任意脚本。
- [x] 1.4 实现 Relay 正式只读查询桥，在 Relay 线程生成快照，区分预期名单与实际事实。
- [x] 1.5 独立实现并注册 help、session.info、actor.list、runtime.status。
- [x] 1.6 完成 HTTP 请求/响应、容量、超时、断线和操作日志的正式运行闭合。

## 2. 独立文本控制台

- [x] 2.1 在额外 GM 进程终端中接入解析、请求提交和结果展示，不写命令业务分支。
- [x] 2.2 完成历史、翻页、连接身份、重连、退出和清屏，绘制不等待网络。
- [x] 2.3 删除游戏内窗口、InputAction、Prefab 引用和设备焦点改动，恢复原 Player HTTP 配置。
- [x] 2.4 状态层和连接迁到 .NET 工具侧，构建配置改为 Editor-only，不保留 Unity 客户端运行依赖。

## 3. 产品与文档

- [x] 3.1 闭合正式构建与启动，GM 窗口可见，Player 不下发工具配置或凭据。
- [x] 3.2 同步实施规格、用法和扩展入口，不捆绑玩法 GM、采样、图形 UI 或 Action 修复。

## 当前实施边界

实现已闭合为额外进程里的文本控制台，游戏内入口全部移除。构建和运行记录见 verification.md，使用说明见 3cDemo/Tools/DeterministicRollback/GmConsole.md。本 change 未自动归档，不改动用户的其它 active change。
