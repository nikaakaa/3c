## 1. 固定合同与保护范围

- [ ] 1.1 对账实施时current specs与相关active结果，记录唯一脚请求/最终结果合同及用户接受的行为、源码、资源和诊断版本基线
- [ ] 1.2 建立Foot/Pelvis/Bend持久字段的写入Owner、消费者、初始化与Reset清单，区分运行历史、帧结果和过程证据
- [ ] 1.3 对齐stabilize的Resolved/Pelvis重叠条款，保留Goal Sole历史、位置basis与Pelvis实验的原Owner，不留下双合同

## 2. 脚请求与最终结果收口

- [ ] 2.1 引入模块内部typed脚请求和Pair，明确身份、未加权目标、有效目标、Support与Reach视图；禁止用最终Resolved表达初步需求
- [ ] 2.2 将Primary Support与既有ResolvePelvis接到请求视图，保持原Support选择、目标、真实Reach、一次响应及配置不变
- [ ] 2.3 把Landing完成凭据限制在Foot内部，以typed Reach Outcome区分原请求满足与受限输出，保留原Full Lock门控
- [ ] 2.4 将既有Foot Reach夹紧归入唯一Foot收口，保持权重、空间转换与数值执行顺序，并统一更新最终Ankle、Sole、Rotation与Correction
- [ ] 2.5 让Goal编码只读最终Resolved及Pelvis Result，删除Goal编码后的业务夹紧、初步Resolved副本和失去消费者的参数
- [ ] 2.6 接入同Frame/Completion/Rig/Side的请求、结果与Goal一致性校验，保持唯一根Bank、Contribution、Assembler、Goal Set及Writer

## 3. 运行历史与过程证据分责

- [ ] 3.1 把CorrectionResponseFact中被下一帧读取的方向迁入最小typed Interpolation历史，保留现有方向限制、分域、残差和Reset语义
- [ ] 3.2 将响应、接触和可达过程Fact改为本帧不可变证据，删除同义状态副本及对公开Diagnostics的运行依赖
- [ ] 3.3 保持Pending开放状态与Committed结果有效性分责，不在本change启用或重设计stabilize拥有的Goal Sole历史接管
- [ ] 3.4 用既有正式录制对账状态搬移前后Goal、Pelvis、Foot连续输出和实际骨骼，保存独立数据差异与版本证据

## 4. 独立修正Solver重置后的方向所有权

- [ ] 4.1 从现有Rig参考姿态与Profile准备过程取得精确typed方向初值及身份，非法准备显式失败，不新增默认轴或近似初始化
- [ ] 4.2 在明确清空Solver历史的初始化、Reset与调参路径统一重建正式方向，每帧仅由正式输入设置Vendor工作字段
- [ ] 4.3 删除空BendHistory时读取旧Vendor方向的路径，保持普通帧弯曲符号、稳定化、Goal应用与权重政策不变
- [ ] 4.4 通过既有正式初始化/Reset入口保存新建与完全重置的对照，以及普通历史帧回归证据；缺少覆盖时保持本项未完成

## 5. 诊断记录与采样映射统一

- [ ] 5.1 将响应、Contact、Support、Reach/Pelvis证据按业务分组，删除多层同义平铺拷贝，保留固定容量和同帧冻结
- [ ] 5.2 建立唯一Editor typed采样列绑定，统一名称、类型、单位、有效性及读写映射，初始化检查重复或缺失定义
- [ ] 5.3 由同一绑定驱动Header、CSV写行、Analyzer读取和必需列校验，删除旧手工映射；几何表保持独立紧凑格式
- [ ] 5.4 集中当前格式identity并接入Sampler、Analyzer和Publisher；真实语义变化升级版本，删除旧reader、别名及补值兼容
- [ ] 5.5 在独立输出目录对账字段、顺序、有效性与现有诊断结果，保持评分维度、权重、分母和公式，保留全部历史原包

## 6. 清理与收尾

- [ ] 6.1 清除被替换类型、未消费字段、旧构造参数和重复映射，确认没有第二生命周期、第二采样器或图外Goal后处理
- [ ] 6.2 按规定参数编译受影响Runtime与Editor并立即关闭build server；仅在正式依赖确实变化时通过精确Corin Build发布匹配产物
- [ ] 6.3 对账正常帧行为保持与Reset边界行为修正，分别记录结果、已知限制和未覆盖输入，不以同一总分替代
- [ ] 6.4 更新本change真实任务状态与最终合同，核对current和active条款不存在旧Resolved/Pelvis含义覆盖，完成严格校验和差异检查
